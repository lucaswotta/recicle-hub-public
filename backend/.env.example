# ==============================================================================
# CONFIGURAÇÃO DE AMBIENTE - RECICLE HUB
# ==============================================================================

# --- CONEXÃO SSH ---
# O sistema conecta-se primeiro a este servidor para criar o túnel seguro.
SSH_HOST=
SSH_PORT=
SSH_USER=
# Caminho para a chave privada
SSH_KEY_PATH=
# Preencha apenas se a chave privada tiver senha
SSH_PASSPHRASE=

# --- BANCO DE DADOS ---
# Postgree Database: Este é o banco para consultar os dados em produção.
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# Oracle Database: Este é o banco para salvar os usuários e logs de auditoria.
ORACLE_USER=
ORACLE_PASSWORD=
ORACLE_HOST=
ORACLE_PORT=
ORACLE_SERVICE=

# --- COMPORTAMENTO DA API ---
# 'development': Mostra erros detalhados e cookies não-seguros (http).
# 'production': Oculta erros, exige HTTPS para cookies e ativa otimizações.
NODE_ENV=development
API_PORT=

# --- SEGURANÇA (JWT) ---
# Chaves para assinar e verificar tokens JWT
ACCESS_TOKEN_SECRET=
REFRESH_TOKEN_SECRET=

# --- RELATÓRIOS EXCEL ---
# Senha padrão para proteger as planilhas geradas contra edição
EXCEL_PASSWORD=

# --- RESET DE SENHA PADRÃO ---
# Quando um admin reseta a senha de um cliente, estas credenciais são aplicadas.
# Exemplo para a senha "Recicle@2026" (Gere o hash usando bcrypt)
RESET_PASSWORD_HASH=
RESET_PASSWORD_SALT=